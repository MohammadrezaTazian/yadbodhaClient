import { message as antdMessage } from 'antd';
import { MessageInstance } from 'antd/es/message/interface';

// Initialize Ant Design message instance
const message: MessageInstance = antdMessage;

export default message;